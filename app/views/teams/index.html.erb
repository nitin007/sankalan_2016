<h2 align="center"> Registered Team Details Page</h2>
<div class="table-responsive">
<table class="table table-striped table-bordered">
  <thead>
    <tr class="info">
      <th>Team name</th>
      <th>Email</th>
      <th>Contact no</th>
      <th>No. of members</th>
    </tr>
  </thead>

  <tbody>
    <% @teams.each do |team| %>
      <tr>
        <td><%= link_to team.team_name, team_path(team) %></td>
        <td><%= team.email %></td>
        <td><%= team.contact_no %></td>
        <td><%= team.members_count %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
